# This project lives on a filesystem that cannot create symlinks (vfat/FAT32
# on the current machine), so npm must not populate node_modules/.bin.
# Every script in package.json therefore invokes its binary through `node`.
# On ext4/APFS/NTFS you can safely delete this file.
bin-links=false
fund=false
audit=false
